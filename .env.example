PORT=8080
NODE_ENV=development

DATABASE_URL=postgresql://usuario:password@localhost:5432/private_forms
DB_POOL_MAX=10

JWT_SECRET=reemplazar_por_un_secret_largo_y_aleatorio
JWT_EXPIRES_IN=2h

ADMIN_EMAIL=admin@tudominio.cl
ADMIN_PASSWORD_HASH=reemplazar_por_hash_bcrypt

FRONTEND_URL=http://localhost:5173
EXTRA_ALLOWED_ORIGINS=http://127.0.0.1:5173

PDF_MAX_FILES=10
PDF_MAX_TOTAL_MB=40
PDF_ADMIN_LINK_EXPIRES_SECONDS=300

GOOGLE_CLOUD_PROJECT_ID=tu-proyecto-google-cloud
GOOGLE_CLOUD_STORAGE_BUCKET=tu-bucket-privado

SMTP_HOST=smtp.tudominio.cl
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=no--reply@tudominio.cl
SMTP_PASS=reemplazar_por_password_smtp

MAIL_FROM="Portafolio <no-reply@tudominio.cl>"
MAIL_ADMIN_TO=admin@tudominio.cl

STORAGE_CLEANUP_LIMIT=25